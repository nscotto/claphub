# cmake/

This folder contains dependencies declarations (like clap) and cmake related code
used accross the project, like function definitions.

The top level [CMakeLists.txt](../CMakeLists.txt) automatically include files with a `.cmake`
extension from here.


[Back to parent README](../README.md)
